apiVersion: v1
kind: ConfigMap
metadata:
  name: additional-config
data:
  # Additional configuration that's not in secrets.yaml
  CORS_ORIGIN: "*"
  LOG_LEVEL: "info"
  RATE_LIMIT_WINDOW_MS: "900000"
  JWT_EXPIRES_IN: "7d"
